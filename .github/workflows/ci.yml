name: CI

on:
  workflow_run:
    workflows:
      - reviewdog
    types:
      - completed
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: test
        run: echo "todo"
